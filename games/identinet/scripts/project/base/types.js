import { SEXES, RACES, ETHNICITIES, EYE_COLORS, HAIR_COLORS, } from "./constants.js";
// value arrays
export const SEX_VALUES = Object.keys(SEXES);
export const RACE_VALUES = Object.keys(RACES);
export const ETHNICITY_VALUES = Object.keys(ETHNICITIES);
export const EYE_COLOR_VALUES = Object.keys(EYE_COLORS);
export const HAIR_COLOR_VALUES = Object.keys(HAIR_COLORS);
